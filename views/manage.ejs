<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Manage Movies</title>
<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f8f9fa; /* Light gray background color */
        margin: 0;
        padding: 0;
    }

    .container {
        max-width: 600px;
        margin: 50px auto;
        padding: 20px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Box shadow for a subtle effect */
    }

    h1 {
        text-align: center;
        color: #ff0000; /* Red heading color */
        margin-bottom: 30px; /* Increased margin bottom */
    }

    h2 {
        text-align: center;
        color: #008000; /* Green color for 'Add New Movie' */
        margin-top: 30px; /* Increased margin top */
    }

    form {
        margin-top: 30px;
    }

    label {
        display: block;
        margin-bottom: 5px;
    }

    input[type="text"], input[type="number"], input[type="file"] {
        width: calc(100% - 10px); /* Adjust width to leave room for borders */
        padding: 10px;
        margin-bottom: 20px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
        font-size: 16px;
    }

    button {
        padding: 10px 20px;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
        transition: background-color 0.3s ease; /* Smooth background color transition */
    }

    button:hover {
        background-color: #0056b3; /* Darker blue on hover */
    }

    .logout-btn {
        margin-top: 20px;
        display: block;
        text-align: center;
        text-decoration: none; /* Remove underline */
        color: #fff; /* Text color */
        background-color: #007bff; /* Button color */
        padding: 10px 20px;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    .logout-btn:hover {
        background-color: #0056b3; /* Darker blue on hover */
    }
</style>
</head>
<body>
<div class="container">
    <h1>Manage Movies</h1>
    <h2>Add New Movie</h2>
    <form action="/manage.ejs" method="post" enctype="multipart/form-data">
        <label for="movieName">Movie Name:</label>
        <input type="text" id="movieName" name="movieName" required>

        <label for="theatreName">Theatre Name:</label>
        <input type="text" id="theatreName" name="theatreName" required>

        <label for="noSeats">Number of Seats:</label>
        <input type="number" id="noSeats" name="noSeats" required>

        <label for="seatsBooked">Seats Booked:</label>
        <input type="number" id="seatsBooked" name="seatsBooked" required>

        <label for="movieImage">Movie Image:</label>
        <input type="file" id="movieImage" name="movieImage" accept="image/*" required>

        <button type="submit">Create</button>
    </form>
    <a href="/crudMenu.ejs" class="logout-btn">Main Menu</a>
</div>
</body>
</html>
